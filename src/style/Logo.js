import styled from 'styled-components';

const Logo = styled.div`
    font-family: 'Saira Stencil One', cursive;
    font-weight: 400;
    font-size: 32px;
    color: #FFFFFF;
`;

export default Logo;